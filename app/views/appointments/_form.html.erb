<%= simple_form_for [@patient, @patient.appointments.build] do |f| %>
  <%= f.input :date, label: "Date" %>
  <%= f.input :time, label: "Time" %>
  <%= f.input :doctor, label: "Doctor" %>
  <%= f.button :submit %>
<% end %>
